DATABASE_URL="postgresql://nasiya:nasiya_dev_password@localhost:5433/nasiya?schema=public"

# NextAuth v5 (Auth.js) — generate with: openssl rand -base64 32
AUTH_SECRET=""
AUTH_URL="http://localhost:3000"

TELEGRAM_BOT_TOKEN=""
TELEGRAM_BOT_USERNAME=""
# Optional: only checked if set — validates the X-Telegram-Bot-Api-Secret-Token
# header on /api/webhooks/telegram against what you passed to setWebhook.
TELEGRAM_WEBHOOK_SECRET=""
