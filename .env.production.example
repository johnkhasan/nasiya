# Copy this to /home/deploy/nasiya/.env on the server (never commit the
# real file — docker-compose.yml reads it for ${VAR} substitution).

DB_PASSWORD=""
DOMAIN="yourdomain.uz"
GHCR_OWNER="johnkhasan"

# openssl rand -base64 32
AUTH_SECRET=""

TELEGRAM_BOT_TOKEN=""
